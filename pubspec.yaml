name: bcam_waveforms
description: A Flutter
version: 0.1.3

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  plugin:
    platforms:
      android:
        package: com.simform.bcam_waveforms
        pluginClass: BcamWaveformsPlugin
      ios:
        pluginClass: BcamWaveformsPlugin

